name: ci-pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9']
    steps:
    # 步骤1：检出代码到CI环境
    - uses: actions/checkout@v4
    
    # 步骤2：配置Python环境
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    # 步骤3：安装项目依赖（包括black格式化工具）
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        # 新增black和pytest
        pip install black pytest pytest-cov
    
    # 步骤4：自动格式化代码（解决格式不规范的问题）
    - name: Auto-format code with Black
      run: black src/ tests/
    
    # 步骤5：检查代码格式（现在格式化后再检查，就会通过）
    - name: Check code format with Black
      run: black --check src/ tests/
    
    # 原本就有的步骤
    - name: Run tests
      run: |
        pytest tests/ --cov=src --cov-report=xml
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.9"
    
    - name: Install dependencies
      run: |
        pip install black flake8
    
    # 先自动格式化所有代码
    - name: Auto-format code with Black
      run: black src/ tests/
    
    # 然后检查格式（应该总是通过，因为已经格式化了）
    - name: Check code formatting
      run: black --check src/ tests/
    
    - name: Lint code
      run: |
        flake8 src/ tests/

  # 新增：模块导入测试
  module-imports:
    runs-on: ubuntu-latest
    needs: [test, lint]  # 依赖其他job完成
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Test module imports
      run: |
        echo "🔍 测试模块导入..."
        python -c "from src.auth.service import AuthService; print('✓ AuthService 导入成功')"
        python -c "from src.database.manager import DatabaseManager; print('✓ DatabaseManager 导入成功')"
        python -c "from src.knowledge.service import KnowledgeService; print('✓ KnowledgeService 导入成功')"
        python -c "from src.scheduler.service import SchedulerService; print('✓ SchedulerService 导入成功')"
        echo "✅ 所有模块导入测试通过！"